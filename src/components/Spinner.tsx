import { SpinnerCircularFixed } from "spinners-react";

const Spinner = () => (
  <SpinnerCircularFixed
    color="rgb(154, 160, 166)"
    secondaryColor="rgba(241, 243, 244, 1)"
  />
);

export default Spinner;
