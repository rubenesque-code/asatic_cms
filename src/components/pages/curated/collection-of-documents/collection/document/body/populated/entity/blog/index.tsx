import Summary from "./Summary";
import { $EntityContainer } from "../_styles";

const Blog = () => {
  return (
    <$EntityContainer>
      <Summary />
    </$EntityContainer>
  );
};

export default Blog;
