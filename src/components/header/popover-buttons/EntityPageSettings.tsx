import { SettingsIcon } from "^components/Icons";
import $IconButton_ from "../_presentation/$IconButton_";

export const HeaderEntityPageSettingsButton = () => {
  return (
    <$IconButton_ tooltip="settings">
      <SettingsIcon />
    </$IconButton_>
  );
};
