import tw from "twin.macro";

export const $PageContainer = tw.div`h-screen flex flex-col overflow-hidden`;

export const $EntityTypeWatermark = tw.h3`text-gray-200 absolute right-0.5 top-0 font-sans text-xs uppercase`;
